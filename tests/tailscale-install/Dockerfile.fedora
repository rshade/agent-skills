FROM fedora:40

RUN dnf install -y curl iproute iptables dnf-plugins-core && dnf clean all

COPY test-install.sh /test-install.sh
RUN chmod +x /test-install.sh

ENV DISTRO=fedora
ENTRYPOINT ["/test-install.sh"]
